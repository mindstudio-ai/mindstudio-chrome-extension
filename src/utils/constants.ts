export enum ACTIONS {
  openDrawer = "openDrawer",
  useUrl = "useUrl",
  submitUrl = "submitUrl",
  useSelection = "useSelection",
  submitSelection = "submitSelection",
}

export enum VIEWS {
  "main" = "main",
  "settings" = "settings",
  "results" = "results",
}

export enum TABS {
  "yourAis" = "yourAis",
  "plugins" = "plugins",
}
