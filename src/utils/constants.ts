export enum ACTIONS {
  openDrawer = "openDrawer",
  loadSelection = "loadSelection",
}

export enum VIEWS {
  "main" = "main",
  "settings" = "settings",
}

export enum TABS {
  "run" = "run",
  "results" = "results",
}
